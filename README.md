# AI_Project
Project for CSCE-420

## Setup

python -m venv .venv

# Windows
.venv\Scripts\activate

# Mac/Linux
source .venv/bin/activate

pip install --upgrade pip
pip install -r requirements/base.txt

# env
Make a .env folder with:
GEMINI_API_KEY = "YOUR_API_KEY"